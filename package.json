{
	"name": "@dawkinsuke/hooks",
	"version": "0.1.2",
	"description": "Claude Code hooks toolkit — write hooks with TypeScript, compile to settings.local.json",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ashigirl96/hx-cli.git"
	},
	"bin": {
		"hx": "src/cli/index.ts"
	},
	"files": [
		"src",
		"templates",
		"bunli.config.ts",
		".bunli"
	],
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./runtime": "./src/runtime.ts"
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"test": "bun test",
		"check": "oxlint --fix . && oxfmt --write .",
		"ci:check": "oxlint . && oxfmt --check .",
		"typecheck": "tsc --noEmit",
		"prepare": "lefthook install",
		"release": "GITHUB_TOKEN=$(gh auth token) release-it --increment=patch"
	},
	"dependencies": {
		"@bunli/core": "^0.8.2",
		"@bunli/plugin-completions": "^0.3.4",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@anthropic-ai/claude-agent-sdk": "latest",
		"@types/bun": "latest",
		"bunli": "^0.8.2",
		"lefthook": "^2.1.4",
		"oxfmt": "latest",
		"oxlint": "latest",
		"release-it": "^19.2.4",
		"typescript": "^5.7.0"
	},
	"peerDependencies": {
		"@anthropic-ai/claude-agent-sdk": ">=0.2.0"
	},
	"peerDependenciesMeta": {
		"@anthropic-ai/claude-agent-sdk": {
			"optional": true
		}
	},
	"engines": {
		"bun": ">=1.0.0"
	}
}
